{
  "name": "freshmart",
  "version": "1.0.0",
  "private": true,
  "description": "ร้านชำเจ๊ดี storefront and Admin PWA",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:repo": "node scripts/check-repo.mjs",
    "check": "npm run check:repo && npm test"
  }
}
